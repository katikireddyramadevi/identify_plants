# Set default behavior to handle line endings.
* text=auto

# Force shell scripts to use LF (Unix) line endings.
*.sh text eol=lf

# Force runtime.txt to use LF so Render reads Python version correctly.
runtime.txt text eol=lf
